apiVersion: apps/v1
kind: Deployment
metadata:
  name: testimonials-grid-section-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: testimonials-grid-section-app
  template:
    metadata:
      labels:
        app: testimonials-grid-section-app
    spec:
      containers:
        - name: testimonials-grid-section-app
          image: testimonials-grid-section-app:latest
          ports:
            - containerPort: 80
          env:
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
