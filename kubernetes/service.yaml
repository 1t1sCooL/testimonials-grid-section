apiVersion: v1
kind: Service
metadata:
  name: testimonials-grid-section
  namespace: default
spec:
  selector:
    app: testimonials-grid-section-app
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
